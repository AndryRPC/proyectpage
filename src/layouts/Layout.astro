---
// Importa estilos globales para que Tailwind v4 y tus estilos se apliquen a todas las páginas
import "../styles/global.css";
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- SEO básico -->
    <title>Academia de Guitarra | Clases Profesionales</title>
    <meta name="description" content="Academia de Guitarra Profesional: clases personalizadas para todos los niveles. Instructores certificados, métodos probados y resultados reales." />
    <meta name="author" content="Guitar Academy" />
    <meta name="theme-color" content="#0f0f0f" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="es_ES" />
    <meta property="og:site_name" content="Guitar Academy" />
    <meta property="og:title" content="Academia de Guitarra | Clases Profesionales" />
    <meta property="og:description" content="Clases premium de guitarra para todos los niveles. Profesores certificados, horarios flexibles y comunidad global." />
    <meta property="og:image" content="/images.ico" />
    <meta property="og:image:alt" content="Guitarra sobre fondo oscuro con acentos dorados" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Academia de Guitarra | Clases Profesionales" />
    <meta name="twitter:description" content="Aprende guitarra con instructores certificados. Resultados garantizados y soporte 24/7." />
    <meta name="twitter:image" content="/images.ico" />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!-- Fuentes -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700;800&family=Inter:wght@300;400;500;600;700&family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  </head>
  <body class="min-h-screen bg-[#0f0f0f] text-[#f0f0f0] antialiased selection:bg-[#d4af37]/20 selection:text-white">
    <!-- Enlace de salto para accesibilidad -->
    <a href="#contenido" class="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-[100] focus:px-4 focus:py-2 focus:bg-black/80 focus:text-white focus:rounded-md">Saltar al contenido</a>

    <!-- Contenedor principal -->
    <div class="relative flex min-h-screen flex-col">
      <!-- Slot para Header/Contenido/Footer -->
      <main id="contenido" class="flex-1 w-full">
        <slot />
      </main>

      <!-- Pie persistente opcional podría ir aquí si se desea un footer global -->
    </div>

    <!-- Scripts opcionales globales podrían añadirse aquí -->
  </body>
</html>

<style is:global>
  /* Variables de diseño globales */
  :root {
    --primary: #d4af37;
    --primary-600: #c7a233;
    --primary-700: #aa8c2c;
    --bg: #0f0f0f;
    --bg-elev: #121212;
    --card: #1a1a1a;
    --text: #f0f0f0;
    --muted: #a0a0a0;
    --ring: rgba(212, 175, 55, 0.5);
    --shadow: 0 10px 30px rgba(0,0,0,0.35);
    --shadow-soft: 0 6px 20px rgba(212, 175, 55, 0.12);
  }

  * { box-sizing: border-box; }
  html, body { margin: 0; padding: 0; width: 100%; }

  h1, h2, h3, h4, h5, h6 {
    font-family: 'Playfair Display', 'Montserrat', serif;
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  a { color: inherit; text-decoration: none; }
  button { border: 0; font-family: inherit; }

  /* Utilidades de enfoque y transiciones */
  :focus-visible { outline: 2px solid var(--primary); outline-offset: 2px; }
  .interactive { transition: transform .25s ease, box-shadow .25s ease, filter .25s ease; }
  .interactive:hover { transform: translateY(-2px) scale(1.02); box-shadow: var(--shadow-soft); filter: brightness(1.02); }
  .interactive:active { transform: translateY(0) scale(.98); }

  /* Animaciones */
  @keyframes float { 0%{transform:translateY(0)} 50%{transform:translateY(-10px)} 100%{transform:translateY(0)} }
  .animate-float { animation: float 3.2s ease-in-out infinite; }
  @keyframes fadeInUp { from{opacity:0; transform: translateY(18px)} to{opacity:1; transform: translateY(0)} }
  .animate-fadeIn { animation: fadeInUp .8s ease both; }
</style>
